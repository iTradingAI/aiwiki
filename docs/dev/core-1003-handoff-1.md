# CORE-1003 Task 1 Handoff Checklist
- Task ID: CORE-1003-1-v1-matrix
- Completed Files: [`tests/fixtures/workspaces/legacy-v1-minimal/aiwiki.yaml`, `tests/fixtures/workspaces/legacy-v1-minimal/02-raw/articles/legacy-source.md`, `tests/fixtures/workspaces/legacy-v1-minimal/05-wiki/source-knowledge/legacy-v1.md`, six CORE_DIRS `.gitkeep` entries, `tests/contracts/cli-compatibility.test.ts`, `docs/dev/core-1003-handoff-1.md`]
- Test Coverage: [`npm run build && node --test dist/tests/contracts/cli-compatibility.test.js` passed 4/4: existing fresh coverage plus v1 default/CJK/space paths. Each v1 path executes `status --json`, `query`, `context`, `next --json`, `plugin list --json`, and `plugin doctor --json`; every command checks unchanged recursive `{path, mtime, sha256}` snapshots and raw Markdown frontmatter. `assertWorkspaceUnchanged` uses `assert.deepEqual` with changed-metadata diagnostics, so inserting a fixture write between a command snapshot pair deterministically fails on the altered path, mtime, or SHA-256; changing only frontmatter also fails its separate raw-text comparison.]
- Pending Integrations: [Task 2 may run `npm run test:compat` and release-check against the completed compiled matrix. No source, version, or dependency changes are required from task 1.]
- Handoff Notes: [The fixture uses the actual workspace legacy alias `schema_version: 1`, retains the default-config field values, includes Markdown entries beneath `02-raw/articles` and `05-wiki/source-knowledge`, and preserves all actual `CORE_DIRS` in the fixture tree. The fresh packed-bin coverage remains intact; v1 uses the packed CLI entrypoint with a true argument array so CJK and space paths are not re-parsed through Windows `cmd.exe`.]
- Sign-off: @Worker-1003-1 2026-08-26

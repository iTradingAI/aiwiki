# CORE-1001 Task 3 Handoff Checklist
- Task ID: CORE-1001-3
- Completed Files: [tests/contracts/extension-api.test.ts, docs/schema/EXTENSION_SCHEMA.md, docs/schema/EXTENSION_SCHEMA.zh-CN.md]
- Test Coverage: {"node --test tests/contracts/extension-api.test.ts": ["packed author API 3/3 pass", "inspect/doctor/command/lint paths leave provider and generator callback counts at 0, persisted in fixture callback-counts.json", "bilingual documentation boundary assertions pass"]}
- Pending Integrations: [CORE-1001-5 contract-matrix integration declaration-only v0.1 deviation record]
- Handoff Notes: Contract tests and schema documentation establish the declaration-only Extension API v0.1 boundary; no `src/` runtime code changes. `aiwiki.extension.v1` remains the compatibility marker. Author-facing types, interfaces, and documentation are stable, while Core 1.0 does not implement production invocation of `contextProviders` or `artifactGenerators`; reconsider production invocation only after the Pro track is restored. `tests/contracts/extension-api.test.ts` creates an enabled local extension fixture with command, lint rule, context provider, and artifact generator callbacks, and the fixture persists callback counters in `callback-counts.json`. The test executes these Core host paths in order: `plugin inspect`, `plugin doctor`, explicit command dispatch, then `lint --json`. After every path it asserts `provider === 0` and `generator === 0`; command and lint counters advance only on their respective supported paths, establishing that no production runtime call reaches declaration-only callbacks. Plugin signing is a proposal requirement transferred to the follow-up Core documentation and migration task; it is explicitly owned by CORE-1002, and this task implements zero signing behavior.
- Sign-off: @Worker-1001-3 2026-08-26

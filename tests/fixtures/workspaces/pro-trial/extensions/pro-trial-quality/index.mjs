import { defineExtension } from "@itradingai/aiwiki/extension-api";

export default defineExtension({
  id: "example.pro-trial-quality",
  name: "Pro trial quality extension",
  version: "0.1.0",
  apiVersion: "aiwiki.extension.v1"
});

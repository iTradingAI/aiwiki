# 安全政策

AIWiki 是一个本地 CLI，会在调用方选择的路径下写入 Markdown、JSON、派生元数据和配置文件。

## 支持版本

| 版本 | 是否支持 |
| --- | --- |
| 0.5.1 | 是 |
| 0.5.0 及更早版本 | 否 |

安全修复会随最新补丁版本交付。如果问题已在最新补丁版本修复，请先升级再报告。

## 安全边界

AIWiki 本身不会上传知识库、抓取网页、绕过登录或付费墙、安装 Obsidian 插件，也不会扫描无关的个人目录。宿主 AI 助手负责读取外部来源，并决定把哪些内容交给 AIWiki。

AIWiki 不是沙箱。它使用调用用户的操作系统权限运行。命令可以写入工作区或其他显式指定的目标路径；显式执行 Agent 同步命令时，也可以更新受支持的本地 Agent 接入文件。请把不可信 payload、本地 extension、工作区路径、符号链接和生成的 Markdown 都视为不可信输入。

需要按安全问题处理的行为包括：路径穿越或符号链接逃逸、写入显式目标之外的路径、意外泄露本地内容、extension 约束失效，或越过文档定义的工作区边界造成损坏。

## 私下报告漏洞

**不要**在公开 GitHub issue、discussion、pull request 或其他公开渠道披露漏洞细节。

请通过 [GitHub 私密漏洞报告](https://github.com/iTradingAI/aiwiki/security/advisories/new) 提交。确认安全后，可附上：

- 受影响的 AIWiki 版本和操作系统；
- 执行的命令及相关参数；
- 预期与实际的路径行为；
- 使用非敏感样例数据的最小复现；
- 影响和已知缓解措施。

如果私密报告表单不可用，请不要公开发布细节。在项目提供可用的私密渠道之前，发布流程会被阻断。

去除密钥、私有路径和敏感知识库内容后，普通的非安全 bug 可以提交到 [GitHub Issues](https://github.com/iTradingAI/aiwiki/issues)。

## 响应预期

维护者力争在 3 个工作日内确认收到私密报告，并在 7 个自然日内给出初步评估。验证、修复和协调披露时间取决于严重程度与可复现性。请在维护者完成调查并准备修复前暂缓公开披露。

详见 [docs/SECURITY.zh-CN.md](docs/SECURITY.zh-CN.md) 完整安全指南。
